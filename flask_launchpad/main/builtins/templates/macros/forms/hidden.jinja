{# ------------------

Hidden field:

{{ hidden(name="name", value="value" }}

#}

{% macro hidden(name="", value="") %}
    <input
    type="hidden"
    name="{{ name }}"
    id="{{ name }}"
    value="{{ value | safe }}"
    />
{% endmacro %}

{# ------------------ #}
