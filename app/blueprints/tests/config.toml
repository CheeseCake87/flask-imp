enabled = "yes"

[settings]
url_prefix = "/tests"
subdomain = ""
url_defaults = {}
static_folder = "static"
template_folder = "templates"
static_url_path = "/tests/static"

[session]
tests_session = "yes"

